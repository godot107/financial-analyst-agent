# Secrets
.env
.env.*
!.env.example

# Saved memos (regenerable)
runs/*
!runs/.gitkeep

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Deploy settings and the last pushed image tag (local)
deploy/config.env
deploy/.image

# Downloaded filings, prices and finished memos (regenerable)
cache/
